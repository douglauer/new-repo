# new-repo

```
usage: ./new_repo.rb [options]
    -n, --name         repository name
    -d, --description  repository description
    --private          make repository private
    --help             display usage
```

